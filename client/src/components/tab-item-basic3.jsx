

const  = ({ className="" }) => {
  return (
    <div className={`w-full relative flex flex-row items-center justify-center py-tabs-paddingsm px-0 box-border text-left text-sm text-pagination-colortext font-heading-heading-3 ${className}`}>
      <div className="relative leading-[22px]">已退回(0)</div>
    </div>);
};

export default ;
