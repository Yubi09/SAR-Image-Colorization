import 'antd/dist/antd.min.css';
import { AutoComplete } from "antd"



const  = ({ className="" }) => {
  return (
    <AutoComplete className={`w-full relative ${className}`} placeholder="Input" />);
};

export default ;
