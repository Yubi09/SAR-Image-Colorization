import 'antd/dist/antd.min.css';
import { Button } from "antd"



const  = ({ className="" }) => {
  return (
    <Button className={`relative ${className}`} type="primary">Button</Button>);
};

export default ;
